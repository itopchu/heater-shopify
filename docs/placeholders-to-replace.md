# Placeholders to replace before deployment

Every placeholder currently seeded in the project. Each entry: **where**, **current value**, **what to replace it with**, **how to replace**, and **why it matters**. Organised by urgency.

Last updated: 2026-04-24. Bump this after every placeholder change.

---

## 🔴 Block launch (must be real before any real traffic)

### 1. Impressum (company legal details)
- **Where**: [agent/scripts/configure-phase-6.mjs](../agent/scripts/configure-phase-6.mjs) — the `/policies/legal-notice` page body. Run script after editing to push updated page to Shopify.
- **Current**: `G-Berg GmbH`, `Musterstraße 12`, `12345 Musterstadt`, email `kontakt@gberg-heizung.de`.
- **Replace with**: real registered address, HRB / Handelsregister number, VAT ID (`USt-IdNr.`), Geschäftsführer (managing director) full name.
- **How**: edit the heredoc strings in the script, re-run `node agent/scripts/configure-phase-6.mjs`. Script is idempotent and updates the existing page.
- **Why**: German §5 TMG requires accurate Impressum. A placeholder here is a fine-able legal violation for a live storefront.

### 2. AGB / Widerrufsbelehrung / Datenschutzerklärung
- **Where**: same file as above — three separate heredocs.
- **Current**: short scaffold text with brand name swapped in but not legally reviewed.
- **Replace with**: AGB and Widerrufsbelehrung generated by a German e-commerce legal service (e.g. eRecht24 or IT-Recht Kanzlei) tailored to G-Berg GmbH. Datenschutzerklärung must reflect actual tools in use (Shopify, Klarna, Google Analytics/Consent Mode if configured, Gemini API on backend — backend tools usually not listed, but legal counsel confirms).
- **How**: paste the generated text into the respective heredocs, re-run the script.
- **Why**: Widerrufsbelehrung wording is legally prescribed; using a scaffold risks voiding buyer rights. Datenschutzerklärung has GDPR penalties if inaccurate.

### 3. Shop display name + sender email
- **Where**: Admin → Settings → Store details (API can't set these in 2026-04).
- **Current**: `heater-dev`, user's Gmail.
- **Replace with**: Name = `G-Berg`, Sender email = `kontakt@gberg-heizung.de` (or real domain inbox once set up).
- **How**: manual Admin UI edit.
- **Why**: sender email appears on every transactional email. Gmail as sender looks unprofessional and triggers spam filters.

### 4. Logo artwork
- **Where**: Admin → Online Store → Themes → G-Berg → Customize → Theme settings → Logo image. Also `theme/config/settings_data.json` key `logo_width` (currently 90).
- **Current**: text-only `G-Berg` in Fraunces font (theme renders the store name as text when logo image is blank).
- **Replace with**: PNG/SVG of the G-Berg wordmark, ideally 600×120px PNG with transparent background, or an SVG.
- **How**: upload via Theme Editor; theme picks it up via `settings.logo`.
- **Why**: brand identity. Text-only is fine for dev, unprofessional for launch.

### 5. Real domain
- **Where**: Admin → Settings → Domains.
- **Current**: `heater-dev.myshopify.com`.
- **Replace with**: production domain (presumably `gberg-heizung.de` — register at your preferred registrar, point DNS at Shopify per their instructions).
- **Why**: no real customer will trust `*.myshopify.com`.

### 6. Production Shopify store
- **Where**: currently everything runs on `heater-dev.myshopify.com` which is a development store. Dev stores can't take real orders.
- **Replace with**: provision a paid plan Shopify store, migrate theme + products + metafield defs + collection structure + shipping zones + market.
- **How**: run `shopify theme push` from a local clone targeting the new prod store; re-run all `agent/scripts/*` one-shot seeders against `--store prod`.
- **Why**: dev stores refuse real orders.

---

## 🟡 Needed before customer-visible soft launch

### 7. WhatsApp chat bubble — phone number ✅ DONE (491722706673, set 2026-04-30)
- **Where**: [theme/config/settings_data.json](../theme/config/settings_data.json) key `whatsapp_phone`. Also editable in Admin → Theme Editor → Theme settings → WhatsApp chat bubble.
- **Current**: `491700000000` (clearly-fake placeholder, German +49 170 mobile format, all trailing zeros).
- **Replace with**: real customer-facing WhatsApp-business number, digits only, international format (example: `491701234567` for +49 170 123 4567).
- **How**: either edit settings_data.json and push, or Admin UI Theme Editor (recommended — merchant-editable without redeploy).
- **Why**: if a customer clicks the bubble with the current placeholder, WhatsApp opens a chat to a non-existent number. Looks broken.

### 8. Homepage hero copy + image
- **Where**: [theme/templates/index.json](../theme/templates/index.json) — `hero` section blocks, and the section's `image` setting (currently empty, falls back to a grey placeholder).
- **Current**: heading "Warmth that feels like home.", subtext "Design radiators for every room — TÜV-tested, 10-year warranty, free EU delivery.", no hero image.
- **Replace with**: real brand copy (may keep the current one if on-brand) and a hero image — ideally a shot of a heated living room with a G-Berg radiator, or a product hero from one of the top SKUs. 1920×800px recommended.
- **How**: Admin → Theme Editor (merchant-editable) or edit index.json + push.
- **Why**: blank hero image is a grey box; first impression matters.

### 9. Brand-accent imagery (favicon, OG image, social share)
- **Where**: Admin → Online Store → Themes → G-Berg → Customize → Theme settings → Favicon + Open Graph image.
- **Current**: Dawn/Shopify defaults.
- **Replace with**: G-Berg favicon (32×32 or 64×64 ICO/PNG) and a 1200×630 OG image.
- **Why**: share links on social, bookmark icons, etc.

---

## 🟢 Pre-launch polish

### 10. Main menu wiring
- **Where**: Admin → Online Store → Navigation → Main menu.
- **Current**: likely the default Dawn menu referencing `/all` or pre-seed collection handles that no longer exist.
- **Replace with**: Heizkörper (dropdown → Badheizkörper, Elektrische Badheizkörper, Austauschheizkörper, Wohnraumheizkörper), Bad (→ Toiletten), Fußbodenheizung (→ Rohre, PE-RT), Zubehör, Ratgeber, Über uns, Heizlastrechner.
- **How**: drag-and-drop in Admin; uses the 11 collections seeded by [agent/scripts/seed-collections.mjs](../agent/scripts/seed-collections.mjs).

### 11. Shopify built-in cookie banner
- **Where**: Admin → Settings → Customer Privacy.
- **Current**: likely enabled (default).
- **Replace with**: disabled. Our theme has its own GDPR-compliant banner in [theme/snippets/consent-banner.liquid](../theme/snippets/consent-banner.liquid).
- **Why**: duplicate banners stack and look broken.

### 12. Product catalog — remaining 30 SKUs
- **Where**: catalog sync pipeline. Currently 25/55 products synced.
- **Current**: 25 products live.
- **Replace with**: full catalog via `npm run sync -- --store dev --limit 55` (requires `IMAGE_GEN_CAP=300` and lifting `SYNC_SKIP_IMAGES=1` from `.env.local` once the image prompt is fixed).
- **Why**: thin catalog hurts conversion and SEO.

### 13. AI-regenerated product images
- **Where**: Gemini image pipeline at [agent/sync/images.ts](../agent/sync/images.ts). Paused via `SYNC_SKIP_IMAGES=1` in `.env.local`.
- **Current**: 95 images across 25 products, quality issues flagged (inconsistent dimensions, object hallucinations, low variety). See [sprint-2-findings.md §17](sprint-2-findings.md).
- **Replace with**: new pass after prompt rework (aspect-ratio pin, product-type-aware context, negative prompt, vision-API validation).
- **Why**: product imagery is the primary conversion driver; wrong objects and inconsistent sizes undermine trust.

### 14. Product descriptions (custom metafields)
- **Where**: sync currently populates `title`, `body_html`, `variants`, `images`, `collections`, plus `sync.*` tracking metafields. It does **not** yet populate the Phase-C custom metafields: `delivery_contents`, `delivery_eta`, `faqs`, `wattage`, `width_cm`, `height_cm`, `ral_color`, `connection_type`, `grundpreis_value`, `grundpreis_unit`.
- **Current**: PDP spec accordion, FAQ, delivery-contents list, filter-by-dimensions all render empty.
- **Replace with**: xxl body-html parser (or upstream metafield mapper) that extracts these fields and writes them during sync.
- **Why**: without this, PDP sections are empty and PLPs have no filters.

### 15. Reseller price markup
- **Where**: [agent/sync/normalize.ts](../agent/sync/normalize.ts) — variants currently mirror xxl's price unchanged.
- **Current**: `variants[].price` copied verbatim from xxl.
- **Replace with**: configurable markup, e.g. `price * 1.05` for a 5% reseller margin. Consider per-category markup (accessories vs radiators).
- **How**: env var `GBERG_MARKUP_MULTIPLIER=1.05` + arithmetic in normalize.
- **Why**: business model requires margin; also price differentiation vs upstream avoids being an exact-price mirror.

### 16. Reviews app decision
- **Where**: [docs/app-decisions.md](app-decisions.md).
- **Current**: not decided. Trust-badges section shows ratings but no review system is wired.
- **Replace with**: Judge.me free tier OR custom `product_review` metaobject (logged in app-decisions.md per project principle #4).

### 17. ES / NL locale publishing
- **Where**: Admin → Settings → Languages.
- **Current**: ES and NL locales exist but are unpublished.
- **Replace with**: publish if you want localized storefronts for those markets (adds Translate & Adapt work for every translatable string).
- **Why**: Europe market includes those countries but buyers currently see EN fallback.

### 18. Primary market swap to Europe
- **Where**: Admin → Settings → Markets.
- **Current**: US is primary (can't be deleted until another market is primary; can't change until paid plan).
- **Replace with**: promote Europe to primary, then delete US.
- **Why**: store is for European reseller business; US as primary market is a leftover from dev-store setup.

---

## 🟢 Sprint 5 closeouts — Admin UI tasks

### S5.2 — Enable PLP filters via Search & Discovery

The metafields are populated (Sprint 4) and JSON-LD product offers expose them (Sprint 5.3). To make filter UI render on PLPs:

1. Visit https://admin.shopify.com/store/heater-dev/apps/search-and-discovery
2. Filters → click **Add filter**
3. Add four filters:
   - Source: **Metafield** → `Product.metafield.custom.width_cm` → Filter type: **Range**
   - Source: **Metafield** → `Product.metafield.custom.height_cm` → Filter type: **Range**
   - Source: **Metafield** → `Product.metafield.custom.wattage` → Filter type: **Range**
   - Source: **Metafield** → `Product.metafield.custom.ral_color` → Filter type: **List**
4. Also enable built-in filters: **Price**, **Vendor**.
5. Save.

Expect: filters render on `/collections/wohnraumheizkoerper`, `/collections/badheizkoerper`, etc., immediately. No theme push required.

### S5.4 — Reviews via Judge.me free tier

Picked over a custom `product_review` metaobject in [docs/app-decisions.md](app-decisions.md) — cheaper, faster, auto-emits review JSON-LD that our `structured-data.liquid` Product block reads when `product.metafields.judgeme.badge` is set.

1. Install: https://apps.shopify.com/judgeme → Add app to **heater-dev** → grant the requested permissions.
2. After install: Judge.me dashboard → Settings → Display → ensure **Product page widget** + **Reviews carousel** are enabled.
3. Theme integration: Judge.me automatically injects its block into the PDP via Theme App Extensions — nothing for us to wire. Verify: open any PDP, scroll past the spec accordion, "Reviews" section should render below.
4. Settings → Review requests → wire to send 7 days after delivery (default is fine for launch).

After install, our `structured-data.liquid` will pick up `product.metafields.reviews.rating` automatically and emit `aggregateRating` schema (gated — emits nothing while review count is 0, which is correct per Google's policy on zero-review ratings).

## Conventions

- Placeholder domains/emails: `example.com`, `example.de`, `kontakt@gberg-heizung.de` (the last is our likely-real domain but not yet confirmed live).
- Placeholder phone numbers: `491700000000` (+49 170 0000000 — valid German mobile format, all trailing zeros signals "fake").
- Placeholder company addresses: `Musterstraße 12, 12345 Musterstadt` (German "placeholder street, placeholder city").
- When adding a new placeholder elsewhere in the repo, **add an entry here in the same commit** and tag it with the file path + line number so future you can find it.
